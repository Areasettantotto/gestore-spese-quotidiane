node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local Edge Function secrets
.env.edge.production.local
supabase/functions/.env.local
supabase/functions/.env*.local

# AI context — local-only (Gestore Spese SaaS). Not Git-tracked; keep on disk.
# Do not broaden to *.md / *.mdc (README.md and other Markdown stay versioned).
docs/**/*.md
.cursor/rules/**/*.mdc

# Local-only mirror tooling + config (path destinazione in mirror.env; non versionare).
.ai-context/
